app:
  port: :8080
  env: test

registry:
  reg: 
    - 
      sourceName: xxx
      namespace: xx
      destinationName: xxxx,xx,xxxx
    - 
      # whole image registry name: quay.io/ruohe/kube-rbac-proxy
      sourceName: xx # like quay.io
      namespace: xx # like ruohe
      destinationName: xx,xxx # like public-registry.io,"registry.hub.docker.com

auth:
  Username: sync
  Password: sync_test

sync:
  routineNum: 2
  retryCount: 2
  filePosition:
    log: xxx/app.log
    auth: xxx/auth.yaml
    images: xxx/images.json
  

rabbitmq:
  queueName: xxx.queue
  info:
    -
      address: "xxxx1:5672"
      username: guest
      password: guest

    -
      address: "xxxx2:5672"
      username: guest
      password: guest